<h1> Vehicles </h1>

<%
=begin%>
 <%= button_to "Add New Car", new_vehicle_path, method: :get %> 
<%
=end%>
<ul>
    <% @vehicles.each do |vehicle| %>
        <li><%= link_to vehicle.make, vehicle_path(vehicle)%></li>
    <%end%>
</ul>